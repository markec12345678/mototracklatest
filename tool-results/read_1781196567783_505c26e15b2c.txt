     1→     1→'use client'
     2→     2→
     3→     3→import { useEffect, useRef, useState, useCallback } from 'react'
     4→     4→import maplibregl from 'maplibre-gl'
     5→     5→import 'maplibre-gl/dist/maplibre-gl.css'
     6→     6→import { useMapStore, type LayerVisibility, getStyleUrl } from '@/lib/map-store'
     7→     7→
     8→     8→// Inject pulsing dot CSS animation
     9→     9→if (typeof document !== 'undefined' && !document.getElementById('geolocation-pulse-style')) {
    10→    10→  const style = document.createElement('style')
    11→    11→  style.id = 'geolocation-pulse-style'
    12→    12→  style.textContent = `
    13→    13→    @keyframes geolocation-pulse {
    14→    14→      0% { box-shadow: 0 0 0 0 rgba(59, 130, 246, 0.5); }
    15→    15→      70% { box-shadow: 0 0 0 12px rgba(59, 130, 246, 0); }
    16→    16→      100% { box-shadow: 0 0 0 0 rgba(59, 130, 246, 0); }
    17→    17→    }
    18→    18→    .geolocation-dot {
    19→    19→      width: 16px;
    20→    20→      height: 16px;
    21→    21→      background: #3b82f6;
    22→    22→      border: 3px solid white;
    23→    23→      border-radius: 50%;
    24→    24→      box-shadow: 0 0 6px rgba(59, 130, 246, 0.4);
    25→    25→      animation: geolocation-pulse 2s ease-out infinite;
    26→    26→    }
    27→    27→  `
    28→    28→  document.head.appendChild(style)
    29→    29→}
    30→    30→
    31→    31→// Layer matching patterns
    32→    32→const LAYER_PATTERNS: Record<keyof LayerVisibility, string[]> = {
    33→    33→  water: ['water'],
    34→    34→  roads: ['road'],
    35→    35→  buildings: ['building'],
    36→    36→  parks: ['park', 'landuse'],
    37→    37→  labels: ['label', 'place'],
    38→    38→}
    39→    39→
    40→    40→function getLayerCategory(layerId: string): keyof LayerVisibility | null {
    41→    41→  const lowerId = layerId.toLowerCase()
    42→    42→  for (const [category, patterns] of Object.entries(LAYER_PATTERNS)) {
    43→    43→    if (patterns.some((pattern) => lowerId.includes(pattern))) {
    44→    44→      return category as keyof LayerVisibility
    45→    45→    }
    46→    46→  }
    47→    47→  return null
    48→    48→}
    49→    49→
    50→    50→export function MapView() {
    51→    51→  const mapContainer = useRef<HTMLDivElement>(null)
    52→    52→  const map = useRef<maplibregl.Map | null>(null)
    53→    53→  const markerRefs = useRef<Map<string, maplibregl.Marker>>(new Map())
    54→    54→  const mapLoadedRef = useRef(false)
    55→    55→  const geolocationMarkerRef = useRef<maplibregl.Marker | null>(null)
    56→    56→  const [mapLoadedVersion, setMapLoadedVersion] = useState(0)
    57→    57→
    58→    58→  // Use refs for values needed in map event handlers to avoid stale closures
    59→    59→  const toolModeRef = useRef(useMapStore.getState().toolMode)
    60→    60→  const selectedMarkerRef = useRef(useMapStore.getState().selectedMarker)
    61→    61→
    62→    62→  // Keep refs in sync
    63→    63→  useEffect(() => {
    64→    64→    const unsub = useMapStore.subscribe((state) => {
    65→    65→      toolModeRef.current = state.toolMode
    66→    66→      selectedMarkerRef.current = state.selectedMarker
    67→    67→    })
    68→    68→    return unsub
    69→    69→  }, [])
    70→    70→
    71→    71→  const currentStyle = useMapStore((s) => s.currentStyle)
    72→    72→  const markers = useMapStore((s) => s.markers)
    73→    73→  const selectedMarker = useMapStore((s) => s.selectedMarker)
    74→    74→  const measurePoints = useMapStore((s) => s.measurePoints)
    75→    75→  const routePoints = useMapStore((s) => s.routePoints)
    76→    76→  const routes = useMapStore((s) => s.routes)
    77→    77→  const geolocation = useMapStore((s) => s.geolocation)
    78→    78→  const layerVisibility = useMapStore((s) => s.layerVisibility)
    79→    79→  const clusteringEnabled = useMapStore((s) => s.clusteringEnabled)
    80→    80→  const buildingExtrusion = useMapStore((s) => s.buildingExtrusion)
    81→    81→  const terrainExaggeration = useMapStore((s) => s.terrainExaggeration)
    82→    82→  const toolMode = useMapStore((s) => s.toolMode)
    83→    83→  const drawings = useMapStore((s) => s.drawings)
    84→    84→  const currentDrawing = useMapStore((s) => s.currentDrawing)
    85→    85→  const drawColor = useMapStore((s) => s.drawColor)
    86→    86→  const drawWidth = useMapStore((s) => s.drawWidth)
    87→    87→
    88→    88→  // Ref for throttling drawing point addition
    89→    89→  const lastDrawTimeRef = useRef(0)
    90→    90→  const isDrawingRef = useRef(false)
    91→    91→
    92→    92→  const markMapLoaded = useCallback((loaded: boolean) => {
    93→    93→    mapLoadedRef.current = loaded
    94→    94→    setMapLoadedVersion((v) => v + 1)
    95→    95→  }, [])
    96→    96→
    97→    97→  // Initialize map (only once)
    98→    98→  useEffect(() => {
    99→    99→    if (!mapContainer.current || map.current) return
   100→   100→
   101→   101→    const newMap = new maplibregl.Map({
   102→   102→      container: mapContainer.current,
   103→   103→      style: getStyleUrl(currentStyle),
   104→   104→      center: [14.5058, 46.0569],
   105→   105→      zoom: 5,
   106→   106→      attributionControl: false,
   107→   107→    })
   108→   108→
   109→   109→    newMap.addControl(new maplibregl.NavigationControl(), 'bottom-right')
   110→   110→    newMap.addControl(new maplibregl.ScaleControl(), 'bottom-left')
   111→   111→    newMap.addControl(
   112→   112→      new maplibregl.AttributionControl({ compact: true }),
   113→   113→      'bottom-right'
   114→   114→    )
   115→   115→
   116→   116→    // Error handling: fall back if MapTiler style fails
   117→   117→    newMap.on('error', () => {
   118→   118→      const style = useMapStore.getState().currentStyle
   119→   119→      if (style.fallbackUrl) {
   120→   120→        console.warn(`Map style "${style.name}" failed to load, using fallback`)
   121→   121→        try {
   122→   122→          const currentStyleUrl = newMap.getStyle()?.name
   123→   123→          // Only fallback if we haven't already fallen back
   124→   124→          if (currentStyleUrl !== style.fallbackUrl) {
   125→   125→            newMap.setStyle(style.fallbackUrl)
   126→   126→          }
   127→   127→        } catch { /* ignore */ }
   128→   128→      }
   129→   129→    })
   130→   130→
   131→   131→    newMap.on('move', () => {
   132→   132→      const center = newMap.getCenter()
   133→   133→      useMapStore.getState().setCenter([center.lng, center.lat])
   134→   134→    })
   135→   135→
   136→   136→    newMap.on('zoom', () => {
   137→   137→      useMapStore.getState().setZoom(newMap.getZoom())
   138→   138→    })
   139→   139→
   140→   140→    newMap.on('rotate', () => {
   141→   141→      useMapStore.getState().setBearing(newMap.getBearing())
   142→   142→    })
   143→   143→
   144→   144→    newMap.on('pitch', () => {
   145→   145→      useMapStore.getState().setPitch(newMap.getPitch())
   146→   146→    })
   147→   147→
   148→   148→    newMap.on('click', (e) => {
   149→   149→      const mode = toolModeRef.current
   150→   150→      if (mode === 'mark') {
   151→   151→        const id = `marker-${Date.now()}`
   152→   152→        useMapStore.getState().addMarker({
   153→   153→          id,
   154→   154→          longitude: e.lngLat.lng,
   155→   155→          latitude: e.lngLat.lat,
   156→   156→          name: `Pin at ${e.lngLat.lat.toFixed(4)}, ${e.lngLat.lng.toFixed(4)}`,
   157→   157→          color: '#ef4444',
   158→   158→          category: 'general',
   159→   159→        })
   160→   160→      } else if (mode === 'measure') {
   161→   161→        useMapStore.getState().addMeasurePoint({
   162→   162→          longitude: e.lngLat.lng,
   163→   163→          latitude: e.lngLat.lat,
   164→   164→        })
   165→   165→      } else if (mode === 'directions') {
   166→   166→        useMapStore.getState().addRoutePoint({
   167→   167→          longitude: e.lngLat.lng,
   168→   168→          latitude: e.lngLat.lat,
   169→   169→        })
   170→   170→      } else {
   171→   171→        useMapStore.getState().setSelectedMarker(null)
   172→   172→      }
   173→   173→    })
   174→   174→
   175→   175→    newMap.on('load', () => {
   176→   176→      markMapLoaded(true)
   177→   177→      // Expose main map reference for minimap
   178→   178→      ;(window as unknown as Record<string, unknown>).__mainMap = newMap
   179→   179→    })
   180→   180→
   181→   181→    newMap.on('styledataloading', () => {
   182→   182→      mapLoadedRef.current = false
   183→   183→    })
   184→   184→
   185→   185→    newMap.on('styledata', () => {
   186→   186→      if (newMap.isStyleLoaded()) {
   187→   187→        markMapLoaded(true)
   188→   188→      }
   189→   189→    })
   190→   190→
   191→   191→    map.current = newMap
   192→   192→
   193→   193→    return () => {
   194→   194→      newMap.remove()
   195→   195→      map.current = null
   196→   196→      mapLoadedRef.current = false
   197→   197→      delete (window as unknown as Record<string, unknown>).__mainMap
   198→   198→    }
   199→   199→  }, [markMapLoaded])
   200→   200→
   201→   201→  // Update map style
   202→   202→  useEffect(() => {
   203→   203→    if (!map.current) return
   204→   204→    mapLoadedRef.current = false
   205→   205→    map.current.setStyle(getStyleUrl(currentStyle))
   206→   206→  }, [currentStyle])
   207→   207→
   208→   208→  // Sync markers (with clustering support)
   209→   209→  useEffect(() => {
   210→   210→    if (!map.current) return
   211→   211→
   212→   212→    const useClustering = clusteringEnabled && markers.length > 5
   213→   213→
   214→   214→    // If using clustering, remove individual markers and add GeoJSON source/layers
   215→   215→    if (useClustering) {
   216→   216→      // Remove all individual markers
   217→   217→      markerRefs.current.forEach((marker) => marker.remove())
   218→   218→      markerRefs.current.clear()
   219→   219→
   220→   220→      const clusterSourceId = 'markers-cluster-source'
   221→   221→      const clusterCircleLayerId = 'markers-cluster-circle'
   222→   222→      const clusterCountLayerId = 'markers-cluster-count'
   223→   223→      const unclusteredLayerId = 'markers-unclustered'
   224→   224→
   225→   225→      // Remove existing layers/sources if they exist
   226→   226→      if (map.current.getLayer(clusterCountLayerId)) map.current.removeLayer(clusterCountLayerId)
   227→   227→      if (map.current.getLayer(clusterCircleLayerId)) map.current.removeLayer(clusterCircleLayerId)
   228→   228→      if (map.current.getLayer(unclusteredLayerId)) map.current.removeLayer(unclusteredLayerId)
   229→   229→      if (map.current.getSource(clusterSourceId)) map.current.removeSource(clusterSourceId)
   230→   230→
   231→   231→      const features: GeoJSON.Feature[] = markers.map((m) => ({
   232→   232→        type: 'Feature',
   233→   233→        properties: {
   234→   234→          id: m.id,
   235→   235→          name: m.name,
   236→   236→          description: m.description || '',
   237→   237→          color: m.color,
   238→   238→          category: m.category,
   239→   239→          selected: selectedMarker === m.id,
   240→   240→        },
   241→   241→        geometry: {
   242→   242→          type: 'Point',
   243→   243→          coordinates: [m.longitude, m.latitude],
   244→   244→        },
   245→   245→      }))
   246→   246→
   247→   247→      map.current.addSource(clusterSourceId, {
   248→   248→        type: 'geojson',
   249→   249→        data: {
   250→   250→          type: 'FeatureCollection',
   251→   251→          features,
   252→   252→        },
   253→   253→        cluster: true,
   254→   254→        clusterRadius: 50,
   255→   255→        clusterMaxZoom: 14,
   256→   256→      })
   257→   257→
   258→   258→      // Cluster circles with size based on point_count
   259→   259→      map.current.addLayer({
   260→   260→        id: clusterCircleLayerId,
   261→   261→        type: 'circle',
   262→   262→        source: clusterSourceId,
   263→   263→        filter: ['has', 'point_count'],
   264→   264→        paint: {
   265→   265→          'circle-color': [
   266→   266→            'step',
   267→   267→            ['get', 'point_count'],
   268→   268→            '#22c55e',
   269→   269→            10,
   270→   270→            '#f59e0b',
   271→   271→            50,
   272→   272→            '#ef4444',
   273→   273→          ],
   274→   274→          'circle-radius': [
   275→   275→            'step',
   276→   276→            ['get', 'point_count'],
   277→   277→            18,
   278→   278→            10,
   279→   279→            24,
   280→   280→            50,
   281→   281→            30,
   282→   282→          ],
   283→   283→          'circle-stroke-width': 3,
   284→   284→          'circle-stroke-color': '#ffffff',
   285→   285→        },
   286→   286→      })
   287→   287→
   288→   288→      // Cluster count labels
   289→   289→      map.current.addLayer({
   290→   290→        id: clusterCountLayerId,
   291→   291→        type: 'symbol',
   292→   292→        source: clusterSourceId,
   293→   293→        filter: ['has', 'point_count'],
   294→   294→        layout: {
   295→   295→          'text-field': '{point_count_abbreviated}',
   296→   296→          'text-font': ['Open Sans Bold'],
   297→   297→          'text-size': 13,
   298→   298→        },
   299→   299→        paint: {
   300→   300→          'text-color': '#ffffff',
   301→   301→        },
   302→   302→      })
   303→   303→
   304→   304→      // Unclustered individual points
   305→   305→      map.current.addLayer({
   306→   306→        id: unclusteredLayerId,
   307→   307→        type: 'circle',
   308→   308→        source: clusterSourceId,
   309→   309→        filter: ['!', ['has', 'point_count']],
   310→   310→        paint: {
   311→   311→          'circle-color': ['get', 'color'],
   312→   312→          'circle-radius': 8,
   313→   313→          'circle-stroke-width': 3,
   314→   314→          'circle-stroke-color': '#ffffff',
   315→   315→        },
   316→   316→      })
   317→   317→
   318→   318→      // Click handler for clusters - zoom into them
   319→   319→      map.current.on('click', clusterCircleLayerId, (e) => {
   320→   320→        const features = map.current!.queryRenderedFeatures(e.point, {
   321→   321→          layers: [clusterCircleLayerId],
   322→   322→        })
   323→   323→        const clusterId = features[0]?.properties?.cluster_id
   324→   324→        if (clusterId !== undefined) {
   325→   325→          const source = map.current!.getSource(clusterSourceId) as maplibregl.GeoJSONSource
   326→   326→          source.getClusterExpansionZoom(clusterId, (err, zoom) => {
   327→   327→            if (err) return
   328→   328→            const geometry = features[0].geometry as GeoJSON.Point
   329→   329→            map.current!.easeTo({
   330→   330→              center: geometry.coordinates as [number, number],
   331→   331→              zoom,
   332→   332→            })
   333→   333→          })
   334→   334→        }
   335→   335→      })
   336→   336→
   337→   337→      // Click handler for unclustered points - show popup
   338→   338→      map.current.on('click', unclusteredLayerId, (e) => {
   339→   339→        const feature = e.features?.[0]
   340→   340→        if (!feature) return
   341→   341→        const props = feature.properties || {}
   342→   342→        const coords = (feature.geometry as GeoJSON.Point).coordinates
   343→   343→
   344→   344→        new maplibregl.Popup({ offset: 15, closeButton: true })
   345→   345→          .setLngLat(coords as [number, number])
   346→   346→          .setHTML(
   347→   347→            `<div style="padding: 8px; min-width: 150px;">
   348→   348→              <h3 style="font-weight: 600; font-size: 14px; margin-bottom: 4px;">${props.name || 'Pin'}</h3>
   349→   349→              ${props.description ? `<p style="font-size: 12px; color: #666; margin-bottom: 4px;">${props.description}</p>` : ''}
   350→   350→              <p style="font-size: 11px; color: #999;">📍 ${coords[1].toFixed(4)}, ${coords[0].toFixed(4)}</p>
   351→   351→            </div>`
   352→   352→          )
   353→   353→          .addTo(map.current!)
   354→   354→
   355→   355→        if (props.id) {
   356→   356→          useMapStore.getState().setSelectedMarker(props.id as string)
   357→   357→        }
   358→   358→      })
   359→   359→
   360→   360→      // Change cursor on hover
   361→   361→      map.current.on('mouseenter', clusterCircleLayerId, () => {
   362→   362→        map.current!.getCanvas().style.cursor = 'pointer'
   363→   363→      })
   364→   364→      map.current.on('mouseleave', clusterCircleLayerId, () => {
   365→   365→        map.current!.getCanvas().style.cursor = ''
   366→   366→      })
   367→   367→      map.current.on('mouseenter', unclusteredLayerId, () => {
   368→   368→        map.current!.getCanvas().style.cursor = 'pointer'
   369→   369→      })
   370→   370→      map.current.on('mouseleave', unclusteredLayerId, () => {
   371→   371→        map.current!.getCanvas().style.cursor = ''
   372→   372→      })
   373→   373→    } else {
   374→   374→      // Not using clustering - clean up cluster layers if they exist, then use individual markers
   375→   375→      const clusterSourceId = 'markers-cluster-source'
   376→   376→      const clusterCircleLayerId = 'markers-cluster-circle'
   377→   377→      const clusterCountLayerId = 'markers-cluster-count'
   378→   378→      const unclusteredLayerId = 'markers-unclustered'
   379→   379→
   380→   380→      if (map.current.getLayer(clusterCountLayerId)) map.current.removeLayer(clusterCountLayerId)
   381→   381→      if (map.current.getLayer(clusterCircleLayerId)) map.current.removeLayer(clusterCircleLayerId)
   382→   382→      if (map.current.getLayer(unclusteredLayerId)) map.current.removeLayer(unclusteredLayerId)
   383→   383→      if (map.current.getSource(clusterSourceId)) map.current.removeSource(clusterSourceId)
   384→   384→
   385→   385→      // Individual marker management
   386→   386→      const existingIds = new Set(markerRefs.current.keys())
   387→   387→      const newMarkerIds = new Set(markers.map((m) => m.id))
   388→   388→
   389→   389→      // Remove markers that no longer exist
   390→   390→      existingIds.forEach((id) => {
   391→   391→        if (!newMarkerIds.has(id)) {
   392→   392→          const marker = markerRefs.current.get(id)
   393→   393→          if (marker) {
   394→   394→            marker.remove()
   395→   395→            markerRefs.current.delete(id)
   396→   396→          }
   397→   397→        }
   398→   398→      })
   399→   399→
   400→   400→      // Add new markers
   401→   401→      markers.forEach((m) => {
   402→   402→        if (!markerRefs.current.has(m.id)) {
   403→   403→          const el = document.createElement('div')
   404→   404→          el.className = 'custom-marker'
   405→   405→          el.innerHTML = `
   406→   406→            <svg width="32" height="40" viewBox="0 0 32 40" fill="none" xmlns="http://www.w3.org/2000/svg">
   407→   407→              <path d="M16 0C7.163 0 0 7.163 0 16c0 12 16 24 16 24s16-12 16-24C32 7.163 24.837 0 16 0z" fill="${m.color}"/>
   408→   408→              <circle cx="16" cy="15" r="6" fill="white"/>
   409→   409→            </svg>
   410→   410→          `
   411→   411→          el.style.cursor = 'pointer'
   412→   412→          el.style.filter =
   413→   413→            selectedMarker === m.id
   414→   414→              ? 'drop-shadow(0 0 6px rgba(0,0,0,0.5))'
   415→   415→              : 'none'
   416→   416→          el.style.transform = selectedMarker === m.id ? 'scale(1.2)' : 'scale(1)'
   417→   417→          el.style.transition = 'transform 0.2s, filter 0.2s'
   418→   418→
   419→   419→          const marker = new maplibregl.Marker({ element: el })
   420→   420→            .setLngLat([m.longitude, m.latitude])
   421→   421→            .setPopup(
   422→   422→              new maplibregl.Popup({ offset: 25, closeButton: true }).setHTML(
   423→   423→                `<div style="padding: 8px; min-width: 150px;">
   424→   424→                  <h3 style="font-weight: 600; font-size: 14px; margin-bottom: 4px;">${m.name}</h3>
   425→   425→                  ${
   426→   426→                    m.description
   427→   427→                      ? `<p style="font-size: 12px; color: #666; margin-bottom: 4px;">${m.description}</p>`
   428→   428→                      : ''
   429→   429→                  }
   430→   430→                  <p style="font-size: 11px; color: #999;">📍 ${m.latitude.toFixed(
   431→   431→                    4
   432→   432→                  )}, ${m.longitude.toFixed(4)}</p>
   433→   433→                </div>`
   434→   434→              )
   435→   435→            )
   436→   436→            .addTo(map.current!)
   437→   437→
   438→   438→          el.addEventListener('click', (e) => {
   439→   439→            e.stopPropagation()
   440→   440→            useMapStore.getState().setSelectedMarker(m.id)
   441→   441→          })
   442→   442→
   443→   443→          markerRefs.current.set(m.id, marker)
   444→   444→        } else {
   445→   445→          // Update existing marker if selected state changed
   446→   446→          const existing = markerRefs.current.get(m.id)
   447→   447→          if (existing) {
   448→   448→            const el = existing.getElement()
   449→   449→            el.style.filter =
   450→   450→              selectedMarker === m.id
   451→   451→                ? 'drop-shadow(0 0 6px rgba(0,0,0,0.5))'
   452→   452→                : 'none'
   453→   453→            el.style.transform = selectedMarker === m.id ? 'scale(1.2)' : 'scale(1)'
   454→   454→          }
   455→   455→        }
   456→   456→      })
   457→   457→    }
   458→   458→
   459→   459→    // Cleanup function for cluster event handlers
   460→   460→    return () => {
   461→   461→      if (map.current) {
   462→   462→        map.current.off('click', 'markers-cluster-circle')
   463→   463→        map.current.off('click', 'markers-unclustered')
   464→   464→        map.current.off('mouseenter', 'markers-cluster-circle')
   465→   465→        map.current.off('mouseleave', 'markers-cluster-circle')
   466→   466→        map.current.off('mouseenter', 'markers-unclustered')
   467→   467→        map.current.off('mouseleave', 'markers-unclustered')
   468→   468→      }
   469→   469→    }
   470→   470→  }, [markers, selectedMarker, clusteringEnabled])
   471→   471→
   472→   472→  // Sync measurement line
   473→   473→  useEffect(() => {
   474→   474→    if (!map.current || !mapLoadedRef.current) return
   475→   475→
   476→   476→    const sourceId = 'measure-source'
   477→   477→    const layerId = 'measure-layer'
   478→   478→    const pointLayerId = 'measure-points-layer'
   479→   479→
   480→   480→    if (!map.current.getSource(sourceId)) {
   481→   481→      map.current.addSource(sourceId, {
   482→   482→        type: 'geojson',
   483→   483→        data: {
   484→   484→          type: 'FeatureCollection',
   485→   485→          features: [],
   486→   486→        },
   487→   487→      })
   488→   488→    }
   489→   489→
   490→   490→    if (!map.current.getLayer(layerId)) {
   491→   491→      map.current.addLayer({
   492→   492→        id: layerId,
   493→   493→        type: 'line',
   494→   494→        source: sourceId,
   495→   495→        paint: {
   496→   496→          'line-color': '#ef4444',
   497→   497→          'line-width': 3,
   498→   498→          'line-dasharray': [2, 2],
   499→   499→        },
   500→   500→      })
   501→   501→    }
   502→   502→
   503→   503→    if (!map.current.getLayer(pointLayerId)) {
   504→   504→      map.current.addLayer({
   505→   505→        id: pointLayerId,
   506→   506→        type: 'circle',
   507→   507→        source: sourceId,
   508→   508→        paint: {
   509→   509→          'circle-radius': 6,
   510→   510→          'circle-color': '#ef4444',
   511→   511→          'circle-stroke-width': 2,
   512→   512→          'circle-stroke-color': '#ffffff',
   513→   513→        },
   514→   514→        filter: ['==', '$type', 'Point'],
   515→   515→      })
   516→   516→    }
   517→   517→
   518→   518→    const features: GeoJSON.Feature[] = measurePoints.map((p) => ({
   519→   519→      type: 'Feature',
   520→   520→      properties: {},
   521→   521→      geometry: {
   522→   522→        type: 'Point',
   523→   523→        coordinates: [p.longitude, p.latitude],
   524→   524→      },
   525→   525→    }))
   526→   526→
   527→   527→    if (measurePoints.length > 1) {
   528→   528→      features.push({
   529→   529→        type: 'Feature',
   530→   530→        properties: {},
   531→   531→        geometry: {
   532→   532→          type: 'LineString',
   533→   533→          coordinates: measurePoints.map((p) => [p.longitude, p.latitude]),
   534→   534→        },
   535→   535→      })
   536→   536→    }
   537→   537→
   538→   538→    const source = map.current.getSource(sourceId) as maplibregl.GeoJSONSource
   539→   539→    source.setData({
   540→   540→      type: 'FeatureCollection',
   541→   541→      features,
   542→   542→    })
   543→   543→  }, [measurePoints, mapLoadedVersion])
   544→   544→
   545→   545→  // Sync route drawing lines and points
   546→   546→  useEffect(() => {
   547→   547→    if (!map.current || !mapLoadedRef.current) return
   548→   548→
   549→   549→    const currentColor = useMapStore.getState().currentRouteColor
   550→   550→
   551→   551→    const sourceId = 'route-source'
   552→   552→    const lineLayerId = 'route-line'
   553→   553→    const pointLayerId = 'route-points'
   554→   554→
   555→   555→    // Add source if not exists
   556→   556→    if (!map.current.getSource(sourceId)) {
   557→   557→      map.current.addSource(sourceId, {
   558→   558→        type: 'geojson',
   559→   559→        data: {
   560→   560→          type: 'FeatureCollection',
   561→   561→          features: [],
   562→   562→        },
   563→   563→      })
   564→   564→    }
   565→   565→
   566→   566→    // Add line layer if not exists
   567→   567→    if (!map.current.getLayer(lineLayerId)) {
   568→   568→      map.current.addLayer({
   569→   569→        id: lineLayerId,
   570→   570→        type: 'line',
   571→   571→        source: sourceId,
   572→   572→        paint: {
   573→   573→          'line-color': currentColor,
   574→   574→          'line-width': 4,
   575→   575→          'line-dasharray': [2, 2],
   576→   576→        },
   577→   577→        filter: ['==', '$type', 'LineString'],
   578→   578→      })
   579→   579→    } else {
   580→   580→      // Update color
   581→   581→      map.current.setPaintProperty(lineLayerId, 'line-color', currentColor)
   582→   582→    }
   583→   583→
   584→   584→    // Add point layer if not exists
   585→   585→    if (!map.current.getLayer(pointLayerId)) {
   586→   586→      map.current.addLayer({
   587→   587→        id: pointLayerId,
   588→   588→        type: 'circle',
   589→   589→        source: sourceId,
   590→   590→        paint: {
   591→   591→          'circle-radius': 7,
   592→   592→          'circle-color': currentColor,
   593→   593→          'circle-stroke-width': 2,
   594→   594→          'circle-stroke-color': '#ffffff',
   595→   595→        },
   596→   596→        filter: ['==', '$type', 'Point'],
   597→   597→      })
   598→   598→    } else {
   599→   599→      map.current.setPaintProperty(pointLayerId, 'circle-color', currentColor)
   600→   600→    }
   601→   601→
   602→   602→    const features: GeoJSON.Feature[] = routePoints.map((p, idx) => ({
   603→   603→      type: 'Feature',
   604→   604→      properties: { index: idx },
   605→   605→      geometry: {
   606→   606→        type: 'Point',
   607→   607→        coordinates: [p.longitude, p.latitude],
   608→   608→      },
   609→   609→    }))
   610→   610→
   611→   611→    if (routePoints.length > 1) {
   612→   612→      features.push({
   613→   613→        type: 'Feature',
   614→   614→        properties: {},
   615→   615→        geometry: {
   616→   616→          type: 'LineString',
   617→   617→          coordinates: routePoints.map((p) => [p.longitude, p.latitude]),
   618→   618→        },
   619→   619→      })
   620→   620→    }
   621→   621→
   622→   622→    const source = map.current.getSource(sourceId) as maplibregl.GeoJSONSource
   623→   623→    source.setData({
   624→   624→      type: 'FeatureCollection',
   625→   625→      features,
   626→   626→    })
   627→   627→
   628→   628→    // Render saved routes
   629→   629→    routes.forEach((route) => {
   630→   630→      const rSourceId = `route-source-${route.id}`
   631→   631→      const rLineLayerId = `route-line-${route.id}`
   632→   632→      const rPointLayerId = `route-points-${route.id}`
   633→   633→
   634→   634→      if (!map.current!.getSource(rSourceId)) {
   635→   635→        map.current!.addSource(rSourceId, {
   636→   636→          type: 'geojson',
   637→   637→          data: {
   638→   638→            type: 'FeatureCollection',
   639→   639→            features: [],
   640→   640→          },
   641→   641→        })
   642→   642→      }
   643→   643→
   644→   644→      if (!map.current!.getLayer(rLineLayerId)) {
   645→   645→        map.current!.addLayer({
   646→   646→          id: rLineLayerId,
   647→   647→          type: 'line',
   648→   648→          source: rSourceId,
   649→   649→          paint: {
   650→   650→            'line-color': route.color,
   651→   651→            'line-width': 4,
   652→   652→            'line-dasharray': [2, 2],
   653→   653→          },
   654→   654→          filter: ['==', '$type', 'LineString'],
   655→   655→        })
   656→   656→      }
   657→   657→
   658→   658→      if (!map.current!.getLayer(rPointLayerId)) {
   659→   659→        map.current!.addLayer({
   660→   660→          id: rPointLayerId,
   661→   661→          type: 'circle',
   662→   662→          source: rSourceId,
   663→   663→          paint: {
   664→   664→            'circle-radius': 7,
   665→   665→            'circle-color': route.color,
   666→   666→            'circle-stroke-width': 2,
   667→   667→            'circle-stroke-color': '#ffffff',
   668→   668→          },
   669→   669→          filter: ['==', '$type', 'Point'],
   670→   670→        })
   671→   671→      }
   672→   672→
   673→   673→      const rFeatures: GeoJSON.Feature[] = route.points.map((p, idx) => ({
   674→   674→        type: 'Feature',
   675→   675→        properties: { index: idx },
   676→   676→        geometry: {
   677→   677→          type: 'Point',
   678→   678→          coordinates: [p.longitude, p.latitude],
   679→   679→        },
   680→   680→      }))
   681→   681→
   682→   682→      if (route.points.length > 1) {
   683→   683→        rFeatures.push({
   684→   684→          type: 'Feature',
   685→   685→          properties: {},
   686→   686→          geometry: {
   687→   687→            type: 'LineString',
   688→   688→            coordinates: route.points.map((p) => [p.longitude, p.latitude]),
   689→   689→          },
   690→   690→        })
   691→   691→      }
   692→   692→
   693→   693→      const rSource = map.current!.getSource(rSourceId) as maplibregl.GeoJSONSource
   694→   694→      rSource.setData({
   695→   695→        type: 'FeatureCollection',
   696→   696→        features: rFeatures,
   697→   697→      })
   698→   698→    })
   699→   699→
   700→   700→    // Cleanup: remove sources/layers for deleted routes
   701→   701→    const currentRouteIds = new Set(routes.map((r) => r.id))
   702→   702→    const style = map.current.getStyle()
   703→   703→    if (style?.layers) {
   704→   704→      for (const layer of style.layers) {
   705→   705→        const match = layer.id.match(/^route-(?:source|line|points)-(.+)$/)
   706→   706→        if (match && !currentRouteIds.has(match[1])) {
   707→   707→          if (map.current!.getLayer(layer.id)) map.current!.removeLayer(layer.id)
   708→   708→          const srcId = `route-source-${match[1]}`
   709→   709→          if (map.current!.getSource(srcId)) map.current!.removeSource(srcId)
   710→   710→        }
   711→   711→      }
   712→   712→    }
   713→   713→
   714→   714→    return () => {
   715→   715→      if (map.current) {
   716→   716→        if (map.current.getLayer(pointLayerId)) map.current.removeLayer(pointLayerId)
   717→   717→        if (map.current.getLayer(lineLayerId)) map.current.removeLayer(lineLayerId)
   718→   718→        if (map.current.getSource(sourceId)) map.current.removeSource(sourceId)
   719→   719→        routes.forEach((route) => {
   720→   720→          const rSourceId = `route-source-${route.id}`
   721→   721→          const rLineLayerId = `route-line-${route.id}`
   722→   722→          const rPointLayerId = `route-points-${route.id}`
   723→   723→          if (map.current!.getLayer(rPointLayerId)) map.current!.removeLayer(rPointLayerId)
   724→   724→          if (map.current!.getLayer(rLineLayerId)) map.current!.removeLayer(rLineLayerId)
   725→   725→          if (map.current!.getSource(rSourceId)) map.current!.removeSource(rSourceId)
   726→   726→        })
   727→   727→      }
   728→   728→    }
   729→   729→  }, [routePoints, routes, mapLoadedVersion])
   730→   730→
   731→   731→  // Geolocation marker with accuracy circle
   732→   732→  useEffect(() => {
   733→   733→    if (!map.current || !mapLoadedRef.current) return
   734→   734→
   735→   735→    const accuracySourceId = 'geolocation-accuracy-source'
   736→   736→    const accuracyLayerId = 'geolocation-accuracy-layer'
   737→   737→
   738→   738→    // Clean up previous geolocation marker
   739→   739→    if (geolocationMarkerRef.current) {
   740→   740→      geolocationMarkerRef.current.remove()
   741→   741→      geolocationMarkerRef.current = null
   742→   742→    }
   743→   743→
   744→   744→    if (!geolocation) {
   745→   745→      // Remove accuracy layers if geolocation is cleared
   746→   746→      if (map.current.getLayer(accuracyLayerId)) {
   747→   747→        map.current.removeLayer(accuracyLayerId)
   748→   748→      }
   749→   749→      if (map.current.getSource(accuracySourceId)) {
   750→   750→        map.current.removeSource(accuracySourceId)
   751→   751→      }
   752→   752→      return
   753→   753→    }
   754→   754→
   755→   755→    const { longitude, latitude, accuracy } = geolocation
   756→   756→
   757→   757→    // Add accuracy circle source and layer
   758→   758→    if (map.current.getSource(accuracySourceId)) {
   759→   759→      const source = map.current.getSource(accuracySourceId) as maplibregl.GeoJSONSource
   760→   760→      source.setData({
   761→   761→        type: 'FeatureCollection',
   762→   762→        features: [
   763→   763→          {
   764→   764→            type: 'Feature',
   765→   765→            properties: {},
   766→   766→            geometry: {
   767→   767→              type: 'Point',
   768→   768→              coordinates: [longitude, latitude],
   769→   769→            },
   770→   770→          },
   771→   771→        ],
   772→   772→      })
   773→   773→    } else {
   774→   774→      map.current.addSource(accuracySourceId, {
   775→   775→        type: 'geojson',
   776→   776→        data: {
   777→   777→          type: 'FeatureCollection',
   778→   778→          features: [
   779→   779→            {
   780→   780→              type: 'Feature',
   781→   781→              properties: {},
   782→   782→              geometry: {
   783→   783→                type: 'Point',
   784→   784→                coordinates: [longitude, latitude],
   785→   785→              },
   786→   786→            },
   787→   787→          ],
   788→   788→        },
   789→   789→      })
   790→   790→    }
   791→   791→
   792→   792→    if (map.current.getLayer(accuracyLayerId)) {
   793→   793→      map.current.removeLayer(accuracyLayerId)
   794→   794→    }
   795→   795→    map.current.addLayer({
   796→   796→      id: accuracyLayerId,
   797→   797→      type: 'circle',
   798→   798→      source: accuracySourceId,
   799→   799→      paint: {
   800→   800→        'circle-radius': {
   801→   801→          stops: [
   802→   802→            [0, 0],
   803→   803→            [20, accuracy / 2],
   804→   804→          ],
   805→   805→          base: 2,
   806→   806→        },
   807→   807→        'circle-color': 'rgba(59, 130, 246, 0.15)',
   808→   808→        'circle-stroke-color': 'rgba(59, 130, 246, 0.3)',
   809→   809→        'circle-stroke-width': 1,
   810→   810→      },
   811→   811→    })
   812→   812→
   813→   813→    // Add pulsing dot marker at user location
   814→   814→    const dotEl = document.createElement('div')
   815→   815→    dotEl.className = 'geolocation-dot'
   816→   816→
   817→   817→    const geoMarker = new maplibregl.Marker({ element: dotEl })
   818→   818→      .setLngLat([longitude, latitude])
   819→   819→      .addTo(map.current)
   820→   820→
   821→   821→    geolocationMarkerRef.current = geoMarker
   822→   822→
   823→   823→    return () => {
   824→   824→      if (geolocationMarkerRef.current) {
   825→   825→        geolocationMarkerRef.current.remove()
   826→   826→        geolocationMarkerRef.current = null
   827→   827→      }
   828→   828→      if (map.current) {
   829→   829→        if (map.current.getLayer(accuracyLayerId)) {
   830→   830→          map.current.removeLayer(accuracyLayerId)
   831→   831→        }
   832→   832→        if (map.current.getSource(accuracySourceId)) {
   833→   833→          map.current.removeSource(accuracySourceId)
   834→   834→        }
   835→   835→      }
   836→   836→    }
   837→   837→  }, [geolocation, mapLoadedVersion])
   838→   838→
   839→   839→  // Sync layer visibility with map
   840→   840→  useEffect(() => {
   841→   841→    if (!map.current || !mapLoadedRef.current) return
   842→   842→
   843→   843→    const style = map.current.getStyle()
   844→   844→    if (!style || !style.layers) return
   845→   845→
   846→   846→    for (const layer of style.layers) {
   847→   847→      const category = getLayerCategory(layer.id)
   848→   848→      if (category !== null) {
   849→   849→        const visible = layerVisibility[category] ? 'visible' : 'none'
   850→   850→        try {
   851→   851→          map.current.setLayoutProperty(layer.id, 'visibility', visible)
   852→   852→        } catch {
   853→   853→          // Layer may have been removed
   854→   854→        }
   855→   855→      }
   856→   856→    }
   857→   857→  }, [layerVisibility, mapLoadedVersion])
   858→   858→
   859→   859→  // Cursor for tool modes
   860→   860→  useEffect(() => {
   861→   861→    if (!map.current) return
   862→   862→
   863→   863→    const canvas = map.current.getCanvas()
   864→   864→    if (toolMode === 'mark' || toolMode === 'measure' || toolMode === 'directions') {
   865→   865→      canvas.style.cursor = 'crosshair'
   866→   866→    } else if (toolMode === 'draw') {
   867→   867→      canvas.style.cursor = 'crosshair'
   868→   868→    } else {
   869→   869→      canvas.style.cursor = ''
   870→   870→    }
   871→   871→  }, [toolMode, mapLoadedVersion])
   872→   872→
   873→   873→  // Drawing interaction - mousedown/mousemove/mouseup for freehand drawing
   874→   874→  useEffect(() => {
   875→   875→    if (!map.current) return
   876→   876→
   877→   877→    const handleMouseDown = (e: maplibregl.MapMouseEvent) => {
   878→   878→      if (toolModeRef.current !== 'draw') return
   879→   879→      // Only respond to left click
   880→   880→      if (e.originalEvent.button !== 0) return
   881→   881→      e.preventDefault()
   882→   882→      isDrawingRef.current = true
   883→   883→      useMapStore.getState().setCurrentDrawing([[e.lngLat.lng, e.lngLat.lat]])
   884→   884→      useMapStore.getState().isDrawing = true
   885→   885→    }
   886→   886→
   887→   887→    const handleMouseMove = (e: maplibregl.MapMouseEvent) => {
   888→   888→      if (!isDrawingRef.current || toolModeRef.current !== 'draw') return
   889→   889→      const now = Date.now()
   890→   890→      if (now - lastDrawTimeRef.current < 30) return // Throttle to ~30ms
   891→   891→      lastDrawTimeRef.current = now
   892→   892→      useMapStore.getState().addDrawingPoint([e.lngLat.lng, e.lngLat.lat])
   893→   893→    }
   894→   894→
   895→   895→    const handleMouseUp = () => {
   896→   896→      if (!isDrawingRef.current) return
   897→   897→      isDrawingRef.current = false
   898→   898→      useMapStore.getState().finishDrawing()
   899→   899→      useMapStore.getState().pushNotification({ type: 'drawing', icon: 'pencil', message: 'Drawing saved' })
   900→   900→    }
   901→   901→
   902→   902→    // Disable map drag when in draw mode
   903→   903→    if (toolMode === 'draw') {
   904→   904→      map.current.dragPan.disable()
   905→   905→    } else {
   906→   906→      map.current.dragPan.enable()
   907→   907→    }
   908→   908→
   909→   909→    map.current.on('mousedown', handleMouseDown)
   910→   910→    map.current.on('mousemove', handleMouseMove)
   911→   911→    map.current.on('mouseup', handleMouseUp)
   912→   912→
   913→   913→    return () => {
   914→   914→      if (map.current) {
   915→   915→        map.current.off('mousedown', handleMouseDown)
   916→   916→        map.current.off('mousemove', handleMouseMove)
   917→   917→        map.current.off('mouseup', handleMouseUp)
   918→   918→        map.current.dragPan.enable()
   919→   919→      }
   920→   920→    }
   921→   921→  }, [toolMode, mapLoadedVersion])
   922→   922→
   923→   923→  // Render current drawing line
   924→   924→  useEffect(() => {
   925→   925→    if (!map.current || !mapLoadedRef.current) return
   926→   926→
   927→   927→    const sourceId = 'draw-current-source'
   928→   928→    const layerId = 'draw-current-line'
   929→   929→
   930→   930→    if (!currentDrawing || currentDrawing.length < 2) {
   931→   931→      // Remove if exists
   932→   932→      if (map.current.getLayer(layerId)) map.current.removeLayer(layerId)
   933→   933→      if (map.current.getSource(sourceId)) map.current.removeSource(sourceId)
   934→   934→      return
   935→   935→    }
   936→   936→
   937→   937→    if (!map.current.getSource(sourceId)) {
   938→   938→      map.current.addSource(sourceId, {
   939→   939→        type: 'geojson',
   940→   940→        data: {
   941→   941→          type: 'FeatureCollection',
   942→   942→          features: [],
   943→   943→        },
   944→   944→      })
   945→   945→    }
   946→   946→
   947→   947→    if (!map.current.getLayer(layerId)) {
   948→   948→      map.current.addLayer({
   949→   949→        id: layerId,
   950→   950→        type: 'line',
   951→   951→        source: sourceId,
   952→   952→        paint: {
   953→   953→          'line-color': drawColor,
   954→   954→          'line-width': drawWidth,
   955→   955→          'line-opacity': 0.8,
   956→   956→          'line-cap': 'round',
   957→   957→          'line-join': 'round',
   958→   958→        },
   959→   959→      })
   960→   960→    } else {
   961→   961→      map.current.setPaintProperty(layerId, 'line-color', drawColor)
   962→   962→      map.current.setPaintProperty(layerId, 'line-width', drawWidth)
   963→   963→    }
   964→   964→
   965→   965→    const source = map.current.getSource(sourceId) as maplibregl.GeoJSONSource
   966→   966→    source.setData({
   967→   967→      type: 'FeatureCollection',
   968→   968→      features: [
   969→   969→        {
   970→   970→          type: 'Feature',
   971→   971→          properties: {},
   972→   972→          geometry: {
   973→   973→            type: 'LineString',
   974→   974→            coordinates: currentDrawing,
   975→   975→          },
   976→   976→        },
   977→   977→      ],
   978→   978→    })
   979→   979→
   980→   980→    return () => {
   981→   981→      if (map.current) {
   982→   982→        if (map.current.getLayer(layerId)) map.current.removeLayer(layerId)
   983→   983→        if (map.current.getSource(sourceId)) map.current.removeSource(sourceId)
   984→   984→      }
   985→   985→    }
   986→   986→  }, [currentDrawing, drawColor, drawWidth, mapLoadedVersion])
   987→   987→
   988→   988→  // Render saved drawings
   989→   989→  useEffect(() => {
   990→   990→    if (!map.current || !mapLoadedRef.current) return
   991→   991→
   992→   992→    // Render each saved drawing as its own source/layer
   993→   993→    drawings.forEach((drawing) => {
   994→   994→      const dSourceId = `draw-source-${drawing.id}`
   995→   995→      const dLayerId = `draw-line-${drawing.id}`
   996→   996→
   997→   997→      if (!map.current!.getSource(dSourceId)) {
   998→   998→        map.current!.addSource(dSourceId, {
   999→   999→          type: 'geojson',
  1000→  1000→          data: {
  1001→  1001→            type: 'FeatureCollection',
  1002→  1002→            features: [],
  1003→  1003→          },
  1004→  1004→        })
  1005→  1005→      }
  1006→  1006→
  1007→  1007→      if (!map.current!.getLayer(dLayerId)) {
  1008→  1008→        map.current!.addLayer({
  1009→  1009→          id: dLayerId,
  1010→  1010→          type: 'line',
  1011→  1011→          source: dSourceId,
  1012→  1012→          paint: {
  1013→  1013→            'line-color': drawing.color,
  1014→  1014→            'line-width': drawing.width,
  1015→  1015→            'line-opacity': 0.85,
  1016→  1016→            'line-cap': 'round',
  1017→  1017→            'line-join': 'round',
  1018→  1018→          },
  1019→  1019→        })
  1020→  1020→      } else {
  1021→  1021→        map.current!.setPaintProperty(dLayerId, 'line-color', drawing.color)
  1022→  1022→        map.current!.setPaintProperty(dLayerId, 'line-width', drawing.width)
  1023→  1023→      }
  1024→  1024→
  1025→  1025→      if (drawing.points.length >= 2) {
  1026→  1026→        const dSource = map.current!.getSource(dSourceId) as maplibregl.GeoJSONSource
  1027→  1027→        dSource.setData({
  1028→  1028→          type: 'FeatureCollection',
  1029→  1029→          features: [
  1030→  1030→            {
  1031→  1031→              type: 'Feature',
  1032→  1032→              properties: {},
  1033→  1033→              geometry: {
  1034→  1034→                type: 'LineString',
  1035→  1035→                coordinates: drawing.points,
  1036→  1036→              },
  1037→  1037→            },
  1038→  1038→          ],
  1039→  1039→        })
  1040→  1040→      }
  1041→  1041→    })
  1042→  1042→
  1043→  1043→    // Cleanup: remove sources/layers for deleted drawings
  1044→  1044→    const currentDrawingIds = new Set(drawings.map((d) => d.id))
  1045→  1045→    const style = map.current.getStyle()
  1046→  1046→    if (style?.layers) {
  1047→  1047→      for (const layer of style.layers) {
  1048→  1048→        const match = layer.id.match(/^draw-line-(drawing-.+)$/) || layer.id.match(/^draw-source-(drawing-.+)$/)
  1049→  1049→        if (match) {
  1050→  1050→          const drawId = match[1]
  1051→  1051→          if (!currentDrawingIds.has(drawId)) {
  1052→  1052→            const layerId = `draw-line-${drawId}`
  1053→  1053→            const srcId = `draw-source-${drawId}`
  1054→  1054→            if (map.current!.getLayer(layerId)) map.current!.removeLayer(layerId)
  1055→  1055→            if (map.current!.getSource(srcId)) map.current!.removeSource(srcId)
  1056→  1056→          }
  1057→  1057→        }
  1058→  1058→      }
  1059→  1059→    }
  1060→  1060→
  1061→  1061→    return () => {
  1062→  1062→      if (map.current) {
  1063→  1063→        drawings.forEach((drawing) => {
  1064→  1064→          const dSourceId = `draw-source-${drawing.id}`
  1065→  1065→          const dLayerId = `draw-line-${drawing.id}`
  1066→  1066→          if (map.current!.getLayer(dLayerId)) map.current!.removeLayer(dLayerId)
  1067→  1067→          if (map.current!.getSource(dSourceId)) map.current!.removeSource(dSourceId)
  1068→  1068→        })
  1069→  1069→      }
  1070→  1070→    }
  1071→  1071→  }, [drawings, mapLoadedVersion])
  1072→  1072→
  1073→  1073→  // 3D Buildings + Terrain
  1074→  1074→  useEffect(() => {
  1075→  1075→    if (!map.current || !mapLoadedRef.current) return
  1076→  1076→
  1077→  1077→    const buildingLayerId = '3d-buildings'
  1078→  1078→    const terrainSourceId = 'terrain-source'
  1079→  1079→
  1080→  1080→    if (buildingExtrusion) {
  1081→  1081→      // --- Terrain ---
  1082→  1082→      try {
  1083→  1083→        if (!map.current.getSource(terrainSourceId)) {
  1084→  1084→          map.current.addSource(terrainSourceId, {
  1085→  1085→            type: 'raster-dem',
  1086→  1086→            url: `https://api.maptiler.com/tiles/terrain-rgb/tiles.json?key=${process.env.NEXT_PUBLIC_MAPTILER_KEY || ''}`,
  1087→  1087→          })
  1088→  1088→        }
  1089→  1089→        map.current.setTerrain({
  1090→  1090→          source: terrainSourceId,
  1091→  1091→          exaggeration: terrainExaggeration,
  1092→  1092→        })
  1093→  1093→      } catch {
  1094→  1094→        // Terrain source may not be available for all styles
  1095→  1095→      }
  1096→  1096→
  1097→  1097→      // --- 3D Buildings ---
  1098→  1098→      const style = map.current.getStyle()
  1099→  1099→      const sources = style?.sources || {}
  1100→  1100→      if (sources['openmaptiles']) {
  1101→  1101→        // Don't add if already present
  1102→  1102→        if (!map.current.getLayer(buildingLayerId)) {
  1103→  1103→          // Detect dark mode
  1104→  1104→          const isDark = document.documentElement.classList.contains('dark')
  1105→  1105→
  1106→  1106→          map.current.addLayer({
  1107→  1107→            id: buildingLayerId,
  1108→  1108→            source: 'openmaptiles',
  1109→  1109→            'source-layer': 'building',
  1110→  1110→            type: 'fill-extrusion',
  1111→  1111→            minzoom: 14,
  1112→  1112→            paint: {
  1113→  1113→              'fill-extrusion-color': isDark ? '#666' : '#ddd',
  1114→  1114→              'fill-extrusion-height': ['get', 'render_height'],
  1115→  1115→              'fill-extrusion-base': ['get', 'render_min_height'],
  1116→  1116→              'fill-extrusion-opacity': isDark ? 0.5 : 0.7,
  1117→  1117→            },
  1118→  1118→          })
  1119→  1119→        }
  1120→  1120→      }
  1121→  1121→    } else {
  1122→  1122→      // Remove terrain
  1123→  1123→      try {
  1124→  1124→        map.current.setTerrain(null)
  1125→  1125→      } catch {
  1126→  1126→        // Ignore if terrain not set
  1127→  1127→      }
  1128→  1128→      try {
  1129→  1129→        if (map.current.getSource(terrainSourceId)) {
  1130→  1130→          map.current.removeSource(terrainSourceId)
  1131→  1131→        }
  1132→  1132→      } catch {
  1133→  1133→        // Ignore
  1134→  1134→      }
  1135→  1135→
  1136→  1136→      // Remove the 3D buildings layer when disabled
  1137→  1137→      if (map.current.getLayer(buildingLayerId)) {
  1138→  1138→        map.current.removeLayer(buildingLayerId)
  1139→  1139→      }
  1140→  1140→    }
  1141→  1141→  }, [buildingExtrusion, terrainExaggeration, mapLoadedVersion])
  1142→  1142→
  1143→  1143→  // Update terrain exaggeration when it changes (while enabled)
  1144→  1144→  useEffect(() => {
  1145→  1145→    if (!map.current || !mapLoadedRef.current || !buildingExtrusion) return
  1146→  1146→    try {
  1147→  1147→      map.current.setTerrain({
  1148→  1148→        source: 'terrain-source',
  1149→  1149→        exaggeration: terrainExaggeration,
  1150→  1150→      })
  1151→  1151→    } catch {
  1152→  1152→      // Terrain not available
  1153→  1153→    }
  1154→  1154→  }, [terrainExaggeration, buildingExtrusion, mapLoadedVersion])
  1155→  1155→
  1156→  1156→  const flyToLocation = useCallback(
  1157→  1157→    (longitude: number, latitude: number, zoom?: number) => {
  1158→  1158→      if (!map.current) return
  1159→  1159→      map.current.flyTo({
  1160→  1160→        center: [longitude, latitude],
  1161→  1161→        zoom: zoom || 14,
  1162→  1162→        duration: 2000,
  1163→  1163→      })
  1164→  1164→    },
  1165→  1165→    []
  1166→  1166→  )
  1167→  1167→
  1168→  1168→  // Expose flyToLocation and resetNorth through window for other components
  1169→  1169→  useEffect(() => {
  1170→  1170→    ;(window as unknown as Record<string, unknown>).__mapFlyTo = flyToLocation
  1171→  1171→    ;(window as unknown as Record<string, unknown>).__mapResetNorth = () => {
  1172→  1172→      if (map.current) {
  1173→  1173→        map.current.easeTo({ bearing: 0, duration: 500 })
  1174→  1174→      }
  1175→  1175→    }
  1176→  1176→    // Export map as image
  1177→  1177→    ;(window as unknown as Record<string, () => void>).__mapExportImage = () => {
  1178→  1178→      if (!map.current) return
  1179→  1179→      const canvas = map.current.getCanvas()
  1180→  1180→      const dataUrl = canvas.toDataURL('image/png')
  1181→  1181→      const link = document.createElement('a')
  1182→  1182→      link.download = `map-export-${Date.now()}.png`
  1183→  1183→      link.href = dataUrl
  1184→  1184→      document.body.appendChild(link)
  1185→  1185→      link.click()
  1186→  1186→      document.body.removeChild(link)
  1187→  1187→    }
  1188→  1188→    return () => {
  1189→  1189→      delete (window as unknown as Record<string, unknown>).__mapFlyTo
  1190→  1190→      delete (window as unknown as Record<string, unknown>).__mapResetNorth
  1191→  1191→      delete (window as unknown as Record<string, unknown>).__mapExportImage
  1192→  1192→    }
  1193→  1193→  }, [flyToLocation])
  1194→  1194→
  1195→  1195→  const isMapLoading = !mapLoadedRef.current && mapLoadedVersion > 0
  1196→  1196→
  1197→  1197→  return (
  1198→  1198→    <div className="relative w-full h-full">
  1199→  1199→      <div
  1200→  1200→        ref={mapContainer}
  1201→  1201→        style={{
  1202→  1202→          position: 'absolute',
  1203→  1203→          top: 0,
  1204→  1204→          bottom: 0,
  1205→  1205→          left: 0,
  1206→  1206→          right: 0,
  1207→  1207→          width: '100%',
  1208→  1208→          height: '100%',
  1209→  1209→        }}
  1210→  1210→      />
  1211→  1211→      {/* Map loading overlay */}
  1212→  1212→      {isMapLoading && (
  1213→  1213→        <div className="absolute inset-0 z-10 pointer-events-none">
  1214→  1214→          <div className="absolute inset-0 map-loading-skeleton opacity-40" />
  1215→  1215→          <div className="absolute top-4 left-1/2 -translate-x-1/2">
  1216→  1216→            <div className="glass rounded-lg px-4 py-2 flex items-center gap-2 shadow-lg">
  1217→  1217→              <div className="h-4 w-4 rounded-full border-2 border-muted-foreground border-t-transparent animate-spin" />
  1218→  1218→              <span className="text-xs font-medium text-muted-foreground">Loading map…</span>
  1219→  1219→            </div>
  1220→  1220→          </div>
  1221→  1221→        </div>
  1222→  1222→      )}
  1223→  1223→    </div>
  1224→  1224→  )
  1225→  1225→}
  1226→  1226→